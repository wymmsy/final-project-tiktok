# Лабораторная работа 1
## Context engineering — AGENTS.md и skill

#### Шаг 0: Выбор кодовой базы

Учебный бэкенд TikTok на .NET 8: три независимых микросервиса — Identity (пользователи, JWT-аутентификация, подписки), Content (видео, звуки, избранное, модерация) и Engagement (лента, лайки, комментарии, репосты, просмотры). Они общаются между собой по gRPC.

- [x] Есть выраженное разделение слоёв
- [x] Размер ~5000 строчек
- [x] Тесты успешно проходятся
- [x] Есть неочевидные, но обнаружимые конвенции
- [x] Популярный стек (C#, Docker)

#### Шаг 1: Baseline запуски

Задача агенту: сделать soft-delete сущности FavouriteVideo. Промпт:

> сделай мягкое удаление (soft delete) сущности FavouriteVideo. не удаляй ничего, пометь сущность как удаленную (is_deleted). также измени код, чтобы эта сущность нигде не использовалась

Было сделано 3 прогона в разных ветках. Агент уточнял, сделать ли удаление из базы данных перед 3 прогоном.

_**Проверка прогонов**_

Был создан скрипт scripts/check_architecture.py, который проходится по файлам .cs и .json и проверяет соответствие правилам. Выводит 3 типа нарушений:
1. `ERROR` — грубые нарушения структуры (импорты из api в domain и application, пустое исключение и т. д.)
2. `WARNING` — не ломает структуру проекта, но плохое решение или нарушение конвенций языка разработки
3. `INFO` — мелкие недочеты, которые не должны идти в прод

Результаты проверки:
<img width="1905" height="568" alt="Screenshot from 2026-09-21 16-05-25" src="https://github.com/user-attachments/assets/352f74dc-14b4-42b4-b35a-7546cf904ff5" />
<img width="1905" height="568" alt="Screenshot from 2026-09-21 16-06-29" src="https://github.com/user-attachments/assets/5e834586-cc4a-4e64-803d-f05d5ad31ce5" />
<img width="1905" height="568" alt="Screenshot from 2026-09-21 16-06-40" src="https://github.com/user-attachments/assets/2331bdd5-1b2d-4e7e-b334-b5b851027e37" />

Эти нарушения были в изначальном проекте и связаны с тем, что мы всё еще учимся!

<img width="1905" height="624" alt="Screenshot from 2026-09-21 16-08-52" src="https://github.com/user-attachments/assets/3094583f-a55a-41cc-bf65-c29d9cad3a25" />

ИИ-агент, в свою очередь, не совершил серьезных ошибок при редактировании кода.

#### Шаг 2: Конвенции репозитория

Сформулированы

#### Шаг 3: AGENTS.md и skill

В файле AGENTS.md сообщается общая информация по проекту, структура файлов и инструкции для сборки и запуска тестов. Далее идут архитектурные правила
связи Domain, Application, Infrastructure и Api. Далее конвенции кода и правила оформления, включая имена переменных, обработку ошибок и временные метки для этапа разработки. Следующие правила касаются коммитов, например, агент должен спрашивать разрешение перед коммитом в origin.

В файле SKILL.md агенту даются инструкции для проверки любых изменений кода. Скрипт из шага 1 запускается после любых изменений сущностей. Помимо порядка действий, в файле прописаны условия применения, таблица правил с расшифровкой, ложные срабатывания и область действия с ограничениями. Файл упрощает проверку коммитов от агента.

#### Шаг 4: Повторение задачи с контекстом

Прогоняем промпт еще раз, но уже с файлами-инструкциями, а далее проходимся скриптом по коду в поисках нарушений. Результат:

<img width="1905" height="540" alt="Screenshot from 2026-09-21 17-20-10" src="https://github.com/user-attachments/assets/8065b797-81a6-41a5-8853-28e7b26bb245" />

Результат проверки не отличается от предыдущих запусков, соответственно, агент продолжает успешно справляться с задачами.

#### Шаг 5: Генерализуемость

Промпт:

> измени код, добавив возможность экспорт данных пользователя («скачать мои данные») — эндпоинт/кнопка собирает данные пользователя из нескольких таблиц/сервисов в один файл (JSON/CSV), с проверкой прав доступа (только свои данные)

Результаты проверки:

<img width="1905" height="540" alt="Screenshot from 2026-09-21 17-36-05" src="https://github.com/user-attachments/assets/1a2af3bb-0279-4fdf-87c1-ff246b5ad4e4" />

Задача потребовала изменения большего количества файлов, чем предыдущая. Несмотря на увеличенную область изменений, агент не совершил ошибок.

### Выводы

То, что во всех прогонах ошибок ноль, само по себе ценный, но не тот вывод, который кажется на первый взгляд: **«AGENTS.md не нужен, агент и так не ошибается»**. Попробуем разобраться подробнее.

Грубый чек-скрипт — это низкая планка, которую современная модель и так не нарушает без дополнительных инструкций. Он хорошо ловит архитектурные нарушения, но не чувствителен к нюансам: полнота выполнения задачи, следование неявным паттернам проекта, git-дисциплина. Если бы вывод лабораторной строился только на счётчике ERROR/WARNING, эксперимент выглядел бы как AGENTS.md ничего не изменил — это **_неверно_**: сравнивать нужно не только нарушил ли агент грубые правила, но и то, что этот скрипт не может проверить.

1. `baseline-attempt-1` и `baseline-attempt-3` дали функционально идентичный код (добавили `IsDeleted`, `HasQueryFilter`, `partial unique index`) — то есть без инструкций в проекте модель уже сходится к одному и тому же решению между независимыми запусками
2. `baseline-attempt-2` — то же решение, но с другим именем метода (`MarkAsDeleted()` вместо `Delete()`) и в `run 3` агент дополнительно уточнил, нужно ли чистить БД — то есть без явных конвенций агент компенсирует их отсутствие вопросом к человеку
3. Все три `baseline`-прогона не выполнили вторую часть промпта — _«измени код, чтобы эта сущность нигде не использовалась»_: контроллер, сервис и репозиторий `FavouriteVideo` остались нетронутыми, только сущность стала мягко удаляемой.
4. `attempt-4` (после появления `AGENTS.md` с описанием слоёв `Api/Application/Infrastructure`) полностью выполнил обе части: тот же soft-delete плюс реально убрал `FavouritesController`, `IFavouriteService`, `FavouriteService`, часть `FavouriteRepository` — то есть закрыл именно то требование промпта, которое три baseline-прогона синхронно проигнорировали.
